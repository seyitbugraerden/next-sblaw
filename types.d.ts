interface BannerProps {
  text: string;
}
interface VisionProps {
  baslik: string;
  text: string;
  icon: string;
}
interface ParamsType {
  params: {
    slug: string;
  };
}

interface CardDataElements {
  img: string;
  title: string;
  description: string;
  url: string;
}
